<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rosstox26.aramis.projectaramis">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegistrationActivity" />
        <activity android:name=".RegistrationActivity_EnterInfo" />
        <activity android:name=".Tutorial" />
        <activity android:name=".TermsConditions" />
        <activity android:name=".MainFeed" />
        <activity android:name=".NavBar_MapView" />
        <activity android:name=".NavBar_Filter" />
        <activity android:name=".BusinessActivity" />
        <activity android:name=".Profile" />
        <activity android:name=".MissionActivity"></activity>
    </application>

</manifest>